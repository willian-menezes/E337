// import configHomolog from './config.homolog';

// import configProduction from './config.production';

// import configStaging from './config.staging';

// const GLOBALS = {
//   APP_PREFIX: 'Wallet',
//   API_VERSION: 'v1',
//   CORE_MOCK_ENV: 'production',
//   PERSIST_VERSION: 'v1',
//   TABBAR_HEIGHT: 64,
// };

// const ENVIRONMENT = {
//   homolog: configHomolog,
//   production: configProduction,
//   staging: configStaging,
// };

// let currentEnv = ENVIRONMENT.production;

// export const setEnv = env => {
//   console.log(`WALLET ENVIRONMENT: ${env}`); // Debug console
//   if (ENVIRONMENT[env]) {
//     currentEnv = ENVIRONMENT[env];
//   }
// };

// export default () => ({ ...GLOBALS, runtime: currentEnv });
